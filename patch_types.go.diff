--- types.go
+++ types.go
@@ -334,8 +334,8 @@
		}
	}

	if len(wordMappers) > 0 {
-		words = applyWordMappers(words, wordMappers...)
+		words = ApplyWordMappers(words, wordMappers...)
	}

	if cfg.upperIndicator != "" {
